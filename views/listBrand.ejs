<div class="container listbrand-container">
    <% if (data.message) { %>
        <div class="alert alert-info">
            <%= data.message %> 
        </div>
    <% } %>
    <h4 class="mb-3 listbrand-title">Danh sách Hãng</h4>

    <div class="d-flex justify-content-between mb-2 listbrand-container">
        <input type="text" id="brand_searchInput" class="form-control w-50 listbrand-searchinput"
            placeholder="🔍 Tìm kiếm hãng..." value="<%= data.search %>">

        <div class="dropdown listbrand-filter-container">
            <button class="btn btn-secondary dropdown-toggle listbrand-filter" type="button" id="filterDropdown"
                data-bs-toggle="dropdown">
                <i class="fa-solid fa-filter"></i> Lọc
            </button>
            <ul class="dropdown-menu listbrand-option">
                <li><a class="dropdown-item listbrand-filter-option" href="?page=1&search=<%= data.search %>">Reset theo STT</a></li>
                <li><a class="dropdown-item listbrand-filter-option" href="?page=1&search=<%= data.search %>&sort=highlight_asc">Độ ưu tiên Tăng dần</a></li>
                <li><a class="dropdown-item listbrand-filter-option" href="?page=1&search=<%= data.search %>&sort=highlight_desc">Độ ưu tiên Giảm dần</a></li>
            </ul>
        </div>
    </div>

    <div class="table-responsive rounded p-2">
        <table class="table table-bordered listbrand-table">
            <thead class="table-light">
                <tr>
                    <th>STT</th>
                    <th>ID</th>
                    <th>Tên thương hiệu</th>
                    <th>Highlight</th>
                    <th colspan="2">Thao tác</th>
                </tr>
            </thead>
            <tbody>
                <% if (data.rows && data.rows.length > 0) { %>
                    <% data.rows.forEach((brand, index) => { %>
                    <tr>
                        <td><%= (data.currentPage - 1) * 5 + index + 1 %></td>
                        <td><%= brand.brand_id %></td>
                        <td><%= brand.name %></td>
                        <td><%= brand.highlight %></td>
                        <td>
                            <a href="/editbrand/<%= brand.brand_id %>" class="btn btn-sm btn-primary">
                                <i class="bi bi-pencil-square"></i> Sửa
                            </a>
                            <form action="/deletebrand" method="POST" style="display:inline-block;" 
                                  onsubmit="return confirm('Bạn có chắc chắn muốn xóa thương hiệu này không?');">
                                <input type="hidden" name="brand_id" value="<%= brand.brand_id %>" />
                                <button class="btn btn-sm btn-danger" type="submit">
                                    <i class="bi bi-trash"></i> Xóa
                                </button>
                            </form>
                        </td>
                    </tr>
                    <% }) %>
                <% } else { %>
                    <tr><td colspan="7">Không có thương hiệu nào được tìm thấy.</td></tr>
                <% } %>
            </tbody>
        </table>
    </div>

    <nav>
        <ul class="pagination justify-content-center listbrand-pagination">
            <li class="page-item <%= data.currentPage === 1 ? 'disabled' : '' %>">
                <a class="page-link" href="?page=<%= data.currentPage - 1 %>">«</a>
            </li>

            <li class="page-item <%= data.currentPage === 1 ? 'active' : '' %>">
                <a class="page-link" href="?page=1">1</a>
            </li>

            <% if (data.currentPage > 4) { %>
                <li class="page-item disabled"><span class="page-link">...</span></li>
            <% } %>

            <% for (let i = Math.max(2, data.currentPage - 2); i <= Math.min(data.totalPages - 1, data.currentPage + 2); i++) { %>
                <li class="page-item <%= data.currentPage === i ? 'active' : '' %>">
                    <a class="page-link" href="?page=<%= i %>"><%= i %></a>
                </li>
            <% } %>

            <% if (data.currentPage < data.totalPages - 3) { %>
                <li class="page-item disabled"><span class="page-link">...</span></li>
            <% } %>

            <% if (data.totalPages > 1) { %>
                <li class="page-item <%= data.currentPage === data.totalPages ? 'active' : '' %>">
                    <a class="page-link" href="?page=<%= data.totalPages %>"><%= data.totalPages %></a>
                </li>
            <% } %>

            <li class="page-item <%= data.currentPage === data.totalPages ? 'disabled' : '' %>">
                <a class="page-link" href="?page=<%= data.currentPage + 1 %>">»</a>
            </li>
        </ul>
    </nav>
</div>

<script>
document.getElementById('brand_searchInput').addEventListener('keypress', function (e) {
    if (e.key === 'Enter') {
        const keyword = e.target.value;
        const urlParams = new URLSearchParams(window.location.search);
        urlParams.set('search', keyword);
        urlParams.set('page', 1);
        window.location.href = `?${urlParams.toString()}`;
    }
});
</script>