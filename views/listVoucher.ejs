<div class="container listvoucher-container">
    <% if (data.message) { %>
        <div class="alert alert-info">
            <%= data.message %> 
        </div>
    <% } %>
    <h4 class="mb-3 listvoucher-title">Danh sách Voucher</h4>

    <div class="d-flex justify-content-between mb-2 listvoucher-container">
        <!-- Ô tìm kiếm -->
        <input type="text" id="voucher_searchInput" class="form-control w-50 listvoucher-searchinput"
            placeholder="🔍 Tìm kiếm theo mã voucher..." value="<%= data.search %>">

        <!-- Dropdown lọc & sắp xếp -->
        <div class="dropdown listvoucher-filter-container">
            <button class="btn btn-secondary dropdown-toggle listvoucher-filter" type="button" id="filterDropdown"
                data-bs-toggle="dropdown">
                <i class="fa-solid fa-filter"></i> Lọc
            </button>
            <ul class="dropdown-menu listvoucher-option">
                <li><a class="dropdown-item listvoucher-filter-option" href="?page=1&search=<%= data.search %>">Reset theo STT</a></li>
                <li><a class="dropdown-item listvoucher-filter-option" href="?page=1&search=<%= data.search %>&sort=value_asc">Giá trị Tăng dần</a></li>
                <li><a class="dropdown-item listvoucher-filter-option" href="?page=1&search=<%= data.search %>&sort=value_desc">Giá trị Giảm dần</a></li>
            </ul>
        </div>
    </div>

    <div class="table-responsive rounded p-2">
        <table class="table table-bordered listvoucher-table">
            <thead class="table-light">
                <tr>
                    <th>STT</th>
                    <th>Mã Voucher</th>
                    <th>Giá trị (%)</th>
                    <th>Ngày tạo</th>
                    <th>Ngày hết hạn</th>
                    <th>Thao tác</th>
                </tr>
            </thead>
            <tbody>
                <% if (data.rows && data.rows.length > 0) { %>
                    <% data.rows.forEach((voucher, index) => { %>
                    <tr>
                        <td><%= (data.currentPage - 1) * 5 + index + 1 %></td>
                        <td><%= voucher.voucher %></td>
                        <td><%= voucher.value %>%</td>
                        <td><%= voucher.create_at %></td>
                        <td><%= voucher.end_at ? voucher.end_at : "Không có" %></td>
                        <td>
                            <a href="/editvoucher/<%= voucher.id %>" class="btn btn-sm btn-primary">
                                <i class="bi bi-pencil-square"></i> Sửa
                            </a>
                            <form action="/deletevoucher" method="POST" style="display:inline-block;" 
                                  onsubmit="return confirm('Bạn có chắc chắn muốn xóa voucher này không?');">
                                <input type="hidden" name="id" value="<%= voucher.id %>" />
                                <button class="btn btn-sm btn-danger" type="submit">
                                    <i class="bi bi-trash"></i> Xóa
                                </button>
                            </form>
                        </td>
                    </tr>
                    <% }) %>
                <% } else { %>
                    <tr><td colspan="6">Không có voucher nào được tìm thấy.</td></tr>
                <% } %>
            </tbody>
        </table>
    </div>

    <!-- Phân trang -->
    <nav>
        <ul class="pagination justify-content-center listvoucher-pagination">
            <li class="page-item <%= data.currentPage === 1 ? 'disabled' : '' %>">
                <a class="page-link" href="?page=<%= data.currentPage - 1 %>">«</a>
            </li>

            <li class="page-item <%= data.currentPage === 1 ? 'active' : '' %>">
                <a class="page-link" href="?page=1">1</a>
            </li>

            <% if (data.currentPage > 4) { %>
                <li class="page-item disabled"><span class="page-link">...</span></li>
            <% } %>

            <% for (let i = Math.max(2, data.currentPage - 2); i <= Math.min(data.totalPages - 1, data.currentPage + 2); i++) { %>
                <li class="page-item <%= data.currentPage === i ? 'active' : '' %>">
                    <a class="page-link" href="?page=<%= i %>"><%= i %></a>
                </li>
            <% } %>

            <% if (data.currentPage < data.totalPages - 3) { %>
                <li class="page-item disabled"><span class="page-link">...</span></li>
            <% } %>

            <% if (data.totalPages > 1) { %>
                <li class="page-item <%= data.currentPage === data.totalPages ? 'active' : '' %>">
                    <a class="page-link" href="?page=<%= data.totalPages %>"><%= data.totalPages %></a>
                </li>
            <% } %>

            <li class="page-item <%= data.currentPage === data.totalPages ? 'disabled' : '' %>">
                <a class="page-link" href="?page=<%= data.currentPage + 1 %>">»</a>
            </li>
        </ul>
    </nav>
</div>

<script>
document.getElementById('voucher_searchInput').addEventListener('keypress', function (e) {
    if (e.key === 'Enter') {
        const keyword = e.target.value;
        const urlParams = new URLSearchParams(window.location.search);
        urlParams.set('search', keyword);
        urlParams.set('page', 1);
        window.location.href = `?${urlParams.toString()}`;
    }
});
</script>