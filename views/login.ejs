<!DOCTYPE html>
<html lang="vi">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin login page</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        .adminlogin-wrapper {
            height: 80vh;
            margin: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            font-family: 'Arial', sans-serif;
            overflow: hidden;
        }

        .adminlogin-container {
            background-color: #ffffff;
            border-radius: 12px;
            box-shadow: 0 6px 15px rgba(0, 0, 0, 0.1);
            padding: 40px;
            width: 100%;
            max-width: 400px;
            text-align: center;
            border-top: 6px solid #218282;

            /* Hiệu ứng fade-in */
            opacity: 0;
            transform: translateY(-30px);
            animation: adminlogin-fadeInDown 0.8s ease-out forwards;
        }

        @keyframes adminlogin-fadeInDown {
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .adminlogin-title {
            font-size: 1.8rem;
            font-weight: bold;
            color: #218282;
            margin-bottom: 20px;
        }

        .adminlogin-form-control {
            border: 1px solid #218282;
            transition: all 0.3s ease;
        }

        .adminlogin-form-control:focus {
            border-color: #218282;
            box-shadow: 0 0 8px rgba(33, 130, 130, 0.5);
        }

        .adminlogin-btn {
            background-color: #218282;
            color: #fff;
            font-weight: bold;
            transition: all 0.3s ease;
        }

        .adminlogin-btn:hover {
            background-color: #186666;
            transform: scale(1.05);
            color: #fff;
        }

        .adminlogin-input-group-text {
            background-color: #218282;
            color: white;
            border: none;
        }

        .adminlogin-input-group-text>i {
            padding: 10px;
        }

        .adminlogin-footer {
            margin-top: 15px;
            font-size: 0.9rem;
        }

        .adminlogin-footer a {
            color: #218282;
            text-decoration: none;
        }

        .adminlogin-footer a:hover {
            text-decoration: underline;
        }
    </style>
</head>


<div class="adminlogin-wrapper">
    <div class="adminlogin-container">
        <div class="adminlogin-title">
            <i class="fas fa-user-shield"></i> Admin Login
        </div>
        <form action="/login" method="POST">
            <div class="mb-3 input-group">
                <span class="adminlogin-input-group-text"><i class="fas fa-user"></i></span>
                <input type="text" name="username" class="form-control adminlogin-form-control" placeholder="Tài khoản"
                    required>
            </div>

            <div class="mb-3 input-group">
                <span class="adminlogin-input-group-text"><i class="fas fa-lock"></i></span>
                <input type="password" name="password" class="form-control adminlogin-form-control"
                    placeholder="Mật khẩu" required>
            </div>

            <button type="submit" class="btn adminlogin-btn w-100">Đăng nhập</button>
        </form>
        <% if (data.error) { %> <!-- Kiểm tra nếu có lỗi -->
            <p class="error-message">
                <%= data.error %>
            </p>
        <% } %>
    </div>

</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>